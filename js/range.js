const range = document.querySelector('.range-controls');
const bar = document.querySelector('.bar');
const min = document.querySelector('.toggle-min');
const max = document.querySelector('.toggle-max');

range.addEventListener('click', function(evt){
        evt.preventDefault();
        console.log(evt.target.)
});